# ChinaMapDrilldown - 中国地图层级下钻 Power BI 自定义视觉

基于 ECharts 的 Power BI 自定义视觉对象，实现中国地图省→市→区三级 Choropleth 填充地图下钻。

## 功能特性

- 三级层级下钻：全国 → 省份 → 城市 → 区县
- 内部点击下钻（无需依赖 Power BI 内置下钻功能）
- 三级面包屑导航（全国 › 省份 › 城市），支持逐级回退
- 标签内容可选：区域名称 / 数值 / 名称+数值（格式面板控制）
- 自定义工具提示字段（悬停显示额外度量）
- 南海诸岛显示模式切换（完整显示 / 小图显示）
- 颜色渐变图例条（可自定义最小/最大值颜色及范围）
- 缩放和平移支持
- 地图 GeoJSON 数据本地打包，Power BI Service 离线可用
- SVG 渲染器，兼容 Power BI Service CSP 安全策略

## 技术栈

- Power BI Custom Visuals SDK (pbiviz v5.5.1, API v5.3.0)
- ECharts 6 (SVG renderer)
- TypeScript (target: ES6)
- 地图数据：阿里云 DataV.GeoAtlas（已打包本地，回退在线获取）

## 版本历史

- **v1.17.0.0** — 恢复并修复表格联动：下钻到省/市时用「多选该区域全部数据行」的方式交叉筛选，使表格过滤到整个省/市（而非之前单选导致的单行），配合数据指纹守卫保证地图下钻不受影响
- **v1.16.0.0** — 定稿「下钻优先」架构：内部缓存渲染地图（省→市→区瞬时下钻），新增数据指纹守卫（update() 中底层数据未变化时跳过重绘），彻底修复 Power BI 重发全量数据导致下钻被重置回全国的问题；省/市级不联动表格（避免表格被过滤成单行），仅区县级（叶子）交叉筛选联动
- **v1.15.0.0** — 移除省份级表格联动，确立下钻优先方案
- **v1.13.0.0** — suppressRender 方案（已被 v1.16 数据指纹守卫取代）
- **v1.12.0.0** — 回退 v1.11 的纯交叉筛选方案（Power BI 交叉筛选不改变本视觉自身数据，导致无法下钻）；恢复内部下钻渲染地图 + 交叉筛选联动表格的组合方案
- **v1.11.0.0** — 架构重构：下钻改为纯交叉筛选驱动（已回退，见 v1.12）
- **v1.10.0.0** — 名称匹配增强（trim + 前缀模糊兜底，修复新疆/西藏部分区域匹配不上）；下钻到省份时触发交叉筛选，联动右侧表格等其他视觉对象
- **v1.9.0.0** — 修复南海诸岛「完整显示」模式下残留小图边框（每次重绘前 chart.clear() + 显式清空 graphic）
- **v1.8.0.0** — 修复直辖市（京津沪渝）下钻：省市同名时跳过地级层直接到区县
- **v1.7.0.0** — 打包全部 33 个省级行政区地图（含港澳），全国任意省份下钻均离线可用
- **v1.6.0.0** — 修复 Service 白屏（require 替代 ES import、SVG 渲染器、viewport 尺寸同步）；移除冗余"标签"数据角色，标签内容由格式面板控制
- **v1.3.0.0** — 初始版本：三级下钻、南海诸岛小图、面包屑导航

## 开发

```bash
# 安装依赖
npm install

# 开发模式（启动本地调试服务器）
npx pbiviz start

# 打包
npx pbiviz package
```

打包后的 `.pbiviz` 文件位于 `dist/` 目录，可导入 Power BI Desktop 使用。

## 数据角色

| 数据角色 | 类型 | 说明 | 最大数量 |
|---------|------|------|---------|
| 省份 (province) | Grouping | 省份/直辖市/自治区 | 1 |
| 城市 (city) | Grouping | 地级市（下钻第二级） | 1 |
| 区县 (district) | Grouping | 区/县（下钻第三级） | 1 |
| 数值 (measure) | Measure | 驱动地图颜色渐变 + 标签显示 | 1 |
| 工具提示 (tooltips) | Measure | 悬停时额外显示的度量字段 | 5 |

## 格式面板设置

| 卡片 | 设置项 |
|------|--------|
| 地图配色 | 最小值颜色、最大值颜色、背景色、边界颜色、最小/最大值范围 |
| 标签设置 | 显示标签、标签内容（名称/数值/名称+数值）、字号、字体颜色 |
| 提示框 | 显示提示框 |
| 地图配置 | 缩放平移、图例条、面包屑导航、南海诸岛显示方式 |

## 使用说明

1. 将省份、城市、区县字段拖入对应数据角色
2. 将度量值（如销售额）拖入「数值」角色
3. 可选：将额外度量（如订单数）拖入「工具提示」
4. 全国地图点击省份 → 下钻到城市；点击城市 → 下钻到区县
5. 面包屑导航支持逐级回退
6. 格式面板中「标签内容」控制地图上显示什么文字

## Power BI Service 部署注意

- 需在管理门户开启「允许使用未经认证的视觉对象」
- 全国 + 全部 33 个省级行政区地图已打包在视觉内部，省级下钻无需网络权限
- 区县级别（第三级）地图按需在线获取

## 已打包地图

全国 + 33 个省级行政区：北京、天津、河北、山西、内蒙古、辽宁、吉林、黑龙江、上海、江苏、浙江、安徽、福建、江西、山东、河南、湖北、湖南、广东、广西、海南、重庆、四川、贵州、云南、西藏、陕西、甘肃、青海、宁夏、新疆、香港、澳门

> 注：台湾省地图 DataV 数据源暂未提供，下钻到该级别时会尝试在线获取。
